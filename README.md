🐍 Python Core — Домашнє завдання №5

Цей репозиторій містить розв’язки завдань модуля GoIT Python Core HW-05, які охоплюють теми замикань, генераторів, функцій вищого порядку та декораторів.

Усі завдання виконані у форматі окремих функцій, структуровані та відповідають технічним вимогам.

📁 Структура проєкту

goit-pycore-hw-05/

├── task1.py 🔁 Кешування чисел Фібоначчі

Функція: caching_fibonacci()

Використовує замикання для збереження стану кешу.

Повертає внутрішню функцію fibonacci(n).

fibonacci(n):

повертає 0, якщо n ≤ 0

повертає 1, якщо n == 1

бере значення з кешу, якщо вже обчислювалося

обчислює рекурсивно та додає в кеш

Забезпечує високу продуктивність при повторних викликах.

├── task2.py 💰 Генератор доходів із тексту

Функції:

generator_numbers(text)

sum_profit(text, func)

generator_numbers():

Використовує регулярні вирази для пошуку чисел у тексті.

Повертає генератор, який yield-ить кожне знайдене число.

Числа вважаються коректно відокремленими пробілами.

sum_profit():

Приймає текст і функцію-генератор.

Підсумовує всі знайдені дійсні числа.

Повертає загальний прибуток як float.

├── task3.py 🧮 Генератор чисел Фібоначчі (необов’язкове)

Функція: fibonacci(n)

Рекурсивний генератор, що повертає числа Фібоначчі.

Для кожного кроку використовує ключове слово yield.

Дає можливість ітерувати послідовність без збереження в пам’яті.

Завдання позначене як необов’язкове, але виконане для повноти.

├── task4.py 🛡️ Декоратор обробки помилок

Функції:

input_error(func) — декоратор

add_contact(), change_contact(), show_phone(), show_all()

parse_input(), main()

input_error:

Обробляє типові помилки:

ValueError

KeyError

IndexError

Повертає дружні текстові повідомлення, замість аварійного завершення програми.

Основна логіка бота:

add — додати контакт

change — оновити телефон

phone — показати номер

all — показати всі контакти

hello — вітання

exit / close — завершення роботи

Усі print / input знаходяться виключно в main().

└── README.md 📝 Опис і структура проєкту

Поточний файл, який пояснює логіку та будову репозиторію.

✨ Автор

Arianna Dolhushyna

📍 Антверпен, Бельгія
💬 Telegram: @ariannnado
🗓️ 2025
