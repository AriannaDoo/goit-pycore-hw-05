# 🐍 Python Core – Домашнє завдання №5

Цей репозиторій містить розв’язки **чотирьох завдань** модуля **GoIT Python Core HW-05**.  
Усі завдання виконані у форматі окремих функцій та перевірені відповідно до вимог.

---

## 📁 Структура проєкту  
goit-pycore-hw-05/

├── **task1.py** 🔁 *Кешування чисел Фібоначчі*  
Функція **`caching_fibonacci()`**
- Використовує **замикання** (closure) для збереження кешу.  
- Повертає внутрішню функцію **`fibonacci(n)`**.  
- `fibonacci(n)`:
  - повертає `0`, якщо *n ≤ 0*  
  - повертає `1`, якщо *n == 1*  
  - повертає значення з кешу, якщо воно вже обчислене  
  - рекурсивно обчислює нові значення та записує в кеш  
- Забезпечує швидке повторне обчислення Fibonacci.

├── **task2.py** 💰 *Генератор доходів із тексту*  
Функції **`generator_numbers(text)`**, **`sum_profit(text, func)`**
- `generator_numbers()`:
  - знаходить усі числа в тексті за допомогою **регулярних виразів**  
  - повертає їх як **генератор**  
  - yield-ить кожне число у форматі float
- `sum_profit()`:
  - отримує текст і функцію-генератор  
  - підсумовує всі знайдені числа  
  - повертає загальний прибуток

├── **task3.py** 🧮 *Генератор чисел Фібоначчі (необов’язкове)*  
Функція **`fibonacci(n)`**
- Рекурсивний **генератор**, який повертає числа Fibonacci через `yield`.  
- Дозволяє ітерувати послідовність без зайвої витрати памʼяті.  
- Завдання є *необов’язковим*, але виконано повністю.

├── **task4.py** 🛡️ *Бот з декоратором для обробки помилок*  
Функції:  
`input_error`, `add_contact`, `change_contact`, `show_phone`, `show_all`,  
`parse_input`, `main`
- `input_error` — декоратор, який обробляє:
  - `ValueError`
  - `KeyError`
  - `IndexError`
- Бот підтримує команди:
  - **hello**
  - **add username phone**
  - **change username phone**
  - **phone username**
  - **all**
  - **close / exit**
- Усі взаємодії з користувачем (`print`, `input`) виконуються **у main()**.

│  
└── **README.md** 📝 Опис і структура проєкту


## ✨ Автор  
**Arianna Dolhushyna**  
📍 Антверпен, Бельгія  
💬 Telegram: **@ariannnado**  
🗓️ 2025
